# Environment untuk API dibaca dari file `.env` di ROOT repo
# (bukan di folder ini). Salin `.env.example` di root menjadi `.env`.
#
#   cp .env.example .env
#
# Saat di-deploy (Railway/Fly/Render), variabel disuntikkan platform
# dan file .env tidak dibutuhkan.
